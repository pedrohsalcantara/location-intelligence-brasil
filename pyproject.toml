[project]
name = "location-intelligence-brasil"
version = "0.1.0"
description = "Pipeline de dados públicos brasileiros para análise territorial e apoio à decisão na expansão empresarial."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.0",
    "pyarrow>=16.0.0",
    "requests>=2.32.0",
]

[tool.uv]
package = false
